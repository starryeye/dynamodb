# Stage 3 - DynamoDB + Spring WebFlux

문서: [Stage 3 - DynamoDB + Spring WebFlux](../../docs/practice/stage3-dynamodb-webflux.md)

이 프로젝트는 Stage 2의 DynamoDB 모델을 유지하면서 실행 모델을 WebFlux와 async DynamoDB client로 바꾸는 애플리케이션이다.

구현 시 자체 `settings.gradle.kts`, `build.gradle.kts`, `docker-compose.yml`, source code, tests를 가진다.

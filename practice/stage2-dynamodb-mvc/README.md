# Stage 2 - DynamoDB + Spring MVC

문서: [Stage 2 - DynamoDB + Spring MVC](../../docs/practice/stage2-dynamodb-mvc.md)

이 프로젝트는 Stage 1과 같은 API를 유지하면서 persistence를 DynamoDB로 바꾸는 애플리케이션이다.

구현 시 자체 `settings.gradle.kts`, `build.gradle.kts`, `docker-compose.yml`, source code, tests를 가진다.

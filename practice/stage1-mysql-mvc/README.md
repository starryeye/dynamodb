# Stage 1 - MySQL + Spring MVC + JPA

문서: [Stage 1 - MySQL + Spring MVC + JPA](../../docs/practice/stage1-mysql-mvc.md)

이 프로젝트는 MySQL/JPA 기반 기준선 애플리케이션이다.

구현 시 자체 `settings.gradle.kts`, `build.gradle.kts`, `docker-compose.yml`, source code, tests를 가진다.

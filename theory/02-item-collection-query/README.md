# 02 Item Collection Query

문서: [Item Collection Query](../../docs/theory/02-item-collection-query.md)

이 프로젝트는 Spring Boot MVC API로 partition key 기반 `Query`를 학습하는 독립 프로젝트다.

목표는 `ownerId`가 같은 item collection을 조회하고 `GetItem`과 `Query`의 차이를 구분하는 것이다.

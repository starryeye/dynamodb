spring:
  application:
    name: theory-02-item-collection-query

app:
  dynamodb:
    region: ${AWS_REGION:ap-northeast-2}
    table-name: ${DYNAMODB_TABLE_NAME:theory_02_item_collection_query}
    endpoint: ${DYNAMODB_ENDPOINT:}
    use-dummy-credentials: ${DYNAMODB_USE_DUMMY_CREDENTIALS:false}

# 05 Sort Key Prefixes

문서: [Sort Key Prefixes](../../docs/theory/05-sort-key-prefixes.md)

이 프로젝트는 Spring Boot MVC 애플리케이션에서 sort key prefix를 학습하는 독립 프로젝트다.

목표는 `TASK#...`, `STATS` 같은 key naming convention을 코드와 테스트로 고정하는 것이다.
